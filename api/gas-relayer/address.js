import { relayerAddressHandler } from '../../../lexie-gas-relayer/src/backend/relayer-proxy-handlers.js';

export const config = {
  api: { bodyParser: false }
};

export default relayerAddressHandler;

